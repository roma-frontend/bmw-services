{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run ESLint",
      "type": "shell",
      "command": "npx eslint . --format json -o report.json",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
